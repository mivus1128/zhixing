# Contributing

感谢参与知行。提交改动前请先阅读 [AGENTS.md](AGENTS.md) 中的隐私与核心行为边界。

## 开发流程

1. 使用独立分支完成改动。
2. 示例只使用虚构数据和 `.invalid` 域名。
3. 运行 `bash scripts/verify.sh`。
4. 确认 `git status` 中没有 `.env`、日志、备份、运行数据或浏览器 profile。
5. 在 Pull Request 中说明行为变化、验证结果和需要关注的兼容性。

涉及券商登录、验证码识别、交易执行、策略生成或自动轮次的改动，必须先取得项目所有者明确授权。仅以“重构”名义改变这些流程也属于行为变更。
