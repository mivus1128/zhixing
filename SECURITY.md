# Security Policy

## 报告安全问题

请优先使用 GitHub 仓库的 Private vulnerability reporting 提交漏洞，不要在公开 Issue 中粘贴账号、密码、Key、Cookie、账户快照或完整日志。

报告中请包含受影响版本、复现条件、预期与实际结果。敏感证据应只提供最小必要片段。

## 部署边界

- 默认网页只监听 `127.0.0.1`，远程访问使用 SSH 隧道。
- 直接通过公网提供服务时，部署者需要在外层配置身份认证和 TLS。
- 运行数据位于 Docker 卷，卷备份应按敏感数据管理。
- 发现凭据泄漏后，应先在对应服务轮换凭据，再处理仓库内容和 Git 历史。

本项目不代管账户或密钥，也不提供公共托管实例。
